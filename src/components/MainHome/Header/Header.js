import React from 'react';
import Home from '../../Home';
import NavBar from '../NavBar/NavBar';

const Header = () => {
    return (
        <div>
            <NavBar></NavBar>
            <Home></Home>
        </div>
    );
};

export default Header;